# lambda-calc2

Lambda calculus interpreter with beta reduction, Church encodings, and normal order evaluation

## Usage

```bash
python3 lambda_calc2.py --help
```

## Features

- Zero dependencies (Python 3.8+ stdlib only)
- Single file implementation
